import { ViajesPipePipe } from './viajes-pipe.pipe';

describe('ViajesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ViajesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
