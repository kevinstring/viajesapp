import { MiReservacionPipe } from './mi-reservacion.pipe';

describe('MiReservacionPipe', () => {
  it('create an instance', () => {
    const pipe = new MiReservacionPipe();
    expect(pipe).toBeTruthy();
  });
});
